# Sample Rest-assured TestNg Gradle Project

This project explains basic Rest-assured gradle setup.

Run `./gradlew test` task to run the tests. Reports can be viewed at `build/reports/tests/index.html`.
